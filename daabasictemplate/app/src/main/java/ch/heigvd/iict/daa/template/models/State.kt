/**
 * Auteur : Koestli Camille / Oliveira Vitoria
 * Description : Ce fichier définit une énumération `State` utilisée pour représenter l'état d'une note.
 * Les états possibles sont `IN_PROGRESS` et `DONE`, permettant de suivre la progression de chaque note.
 */

package ch.heigvd.iict.daa.labo4.models

enum class State {
    IN_PROGRESS, DONE
}