<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sorting options -->
    <item
        android:id="@+id/sort_action"
        android:title="@string/sort"
        android:icon="@drawable/sort" >

        <menu>
            <item
                android:id="@+id/sort_creation_date_action"
                android:title="@string/sort_by_creation_date" />
            <item
                android:id="@+id/sort_schedule_date_action"
                android:title="@string/sort_by_schedule_date" />
        </menu>
    </item>

    <!-- Actions -->
    <item
        android:id="@+id/generate_note_action"
        android:title="@string/generate_action" />
    <item
        android:id="@+id/delete_all_notes_action"
        android:title="@string/delete_action" />
</menu>
